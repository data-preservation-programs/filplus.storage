// ///////////////////////////////////////////////////////////////////// Imports
// -----------------------------------------------------------------------------

// ////////////////////////////////////////////////////////////////////// Export
// -----------------------------------------------------------------------------
module.exports = async (labels, regex) => {
  try {
    return labels.some((label) => { return label.match(regex) })
  } catch (e) {
    console.log('=========================================== [Logic: HasLabel]')
    console.log(e)
    throw e
  }
}
